#include "holberton.h"

/**
 * _strcat - DxC3=D
 *@dest: ni dea
 *@src: img src? xddd
 *
 *Return: char
 */
char *_strcat(char *dest, char *src)
{
	int i = 0, j = 0;

	i = sizeof(dest) / sizeof(char);
	j = sizeof(src) / sizeof(char);

	printf("HOLA: %i, %i\n", i, j);

	return dest;
}
